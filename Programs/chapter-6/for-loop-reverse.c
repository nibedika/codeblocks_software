#include<stdio.h>

int main()
{
    int i;

    // i এর মান নেওয়া, লিমিট সেট এবং i এর মান কমানো
    for(i=5; i>=1; i--)
    {
        // সংখ্যাটি প্রিন্ট করা
        printf("%d\n", i);
    }
    return 0;
}
